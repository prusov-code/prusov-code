name: generate-snake

on:
  schedule:
    - cron: "0 */8 * * *"   # каждые 8 часов
  workflow_dispatch:        # можно запустить вручную

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write   
      pages: write
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4   # ← обновлено до v4

      - name: Generate snake animation
        uses: Platane/snk@v3         # ← основная версия, а не svg-only
        id: snake-gif
        with:
          github_user_name: ${{ github.repository_owner }}
          outputs: |
            dist/github-snake-purple.gif?color_snake=%23AA6FF9&color_dots=%232e1a3a,%234a2b5e,%236b3e8a,%238c52b6,%23ad6ed9
      - name: Push to output branch
        uses: crazy-max/ghaction-github-pages@v4.0.0   # обновлённая версия
        with:
          target_branch: output
          build_dir: dist
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
